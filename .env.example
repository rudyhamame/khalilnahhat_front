# Optional: set these only if you want to override the automatic runtime detection.
# Local/LAN hosts default to http://<current-host>:4000/api
# Remote hosts default to https://djkhalilnahhat-back.onrender.com/api
# VITE_API_BASE_URL=
# VITE_BOOKING_API_URL=
